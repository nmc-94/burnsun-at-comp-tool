# Same-origin in production: leave empty so the client uses a relative /api path.
VITE_API_BASE=
# Dev only: where `npm run dev` proxies /api. Point it at the running backend.
VITE_DEV_API_PROXY_TARGET=http://localhost:8000
